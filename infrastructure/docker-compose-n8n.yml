version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: cyrus_n8n_db
    restart: always
    environment:
      - POSTGRES_USER=cyrus_admin
      - POSTGRES_PASSWORD=cyrus_vault_pass
      - POSTGRES_DB=n8n_data
    volumes:
      - n8n_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cyrus_admin -d n8n_data"]
      interval: 5s
      timeout: 5s
      retries: 5

  n8n:
    image: docker.n8n.io/n8n/n8n
    container_name: cyrus_n8n_automation
    restart: always
    ports:
      - "5678:5678"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_DATABASE=n8n_data
      - DB_POSTGRESDB_HOST=db
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_USER=cyrus_admin
      - DB_POSTGRESDB_PASSWORD=cyrus_vault_pass
      
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - NODE_ENV=production
      
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=CyrusAutomation123!
      
    volumes:
      - n8n_app_data:/home/node/.n8n

volumes:
  n8n_db_data:
  n8n_app_data:
